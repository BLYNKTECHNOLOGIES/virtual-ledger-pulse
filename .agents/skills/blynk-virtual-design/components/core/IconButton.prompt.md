Icon-only control for toolbars, table rows and card corners.

```jsx
<IconButton icon="settings" label="Settings" />
<IconButton icon="plus" variant="primary" label="Add device" />
<IconButton icon="more-horizontal" variant="ghost" size="sm" label="More" />
```

Use `inverse` on ink or brand backgrounds.
