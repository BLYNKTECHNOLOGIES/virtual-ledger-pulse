Filter/selection chip. Squarer than `Badge` — use `Badge` for read-only status, `Tag` for things the user can toggle or dismiss.

```jsx
<Tag selected>Gateways</Tag>
<Tag onRemove={() => {}}>site: hamburg</Tag>
```
