Standard text control. Pair with `Field` for labels.

```jsx
<Input placeholder="Search devices" iconLeft="search" />
<Input type="password" invalid />
```
Placeholders are sentence case and describe the input, not the action.
