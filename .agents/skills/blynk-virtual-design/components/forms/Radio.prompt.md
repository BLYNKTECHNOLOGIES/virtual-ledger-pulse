Exclusive choice among 2–3 short options; above that use `Select`.

```jsx
<Radio value="hourly" checked={p==="hourly"} onChange={setP} label="Hourly" />
<Radio value="daily"  checked={p==="daily"}  onChange={setP} label="Daily" />
```
