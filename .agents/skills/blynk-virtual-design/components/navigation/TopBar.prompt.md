Page header for console views. Glass fill over scrolling content.

```jsx
<TopBar title="Devices" breadcrumb={["Fleet","Hamburg"]} onSearch={setQ}
  actions={<Button size="sm" iconLeft="plus">Add device</Button>} />
```
