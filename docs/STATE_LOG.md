- 2026-09-03: HRMS sidebar text contrast improved — unselected parent/child nav labels, group headers, collapse control, and collapsed hover tooltips now use semantically-opaque foreground tones instead of `text-muted-foreground`, making labels readable against the dark navy sidebar while preserving active-state highlighting.
- 2026-08-23: Attendance policy LOP thresholds wired into payroll — `hr_attendance_policies.late_count_for_lop` and `half_day_count_for_lop` now drive extra LOP days in `hr_lop_days` and `hr_lop_days_window`; a value of 0 disables the corresponding deduction.
- 2026-08-20: First-login password reset loop repaired — successful forced, recovery-link, and OTP resets now atomically clear the ERP temporary-password requirement through an authenticated self-only completion action.
- 2026-08-20: Comp-off ledger repaired and backfilled — phantom bulk/manual allocations removed, current balances rebuilt from verified monthly weekly-off/holiday credits, manual CO edits blocked, and all profile surfaces switched from cumulative to monthly display.
- 2026-08-20: HR Document Studio PDF opening hardened — private Supabase PDFs are downloaded through the authenticated Storage client and opened from a local blob URL, avoiding Chrome/client blocking of signed storage URLs.
- 2026-08-20: HR Document Studio locked-DOCX PDF pipeline corrected — PDFs now render the final merged Word file itself, preserving its native letterhead/header/footer without applying a second ERP letterhead; legacy DOCX PDFs rebuild once as versioned native-Word PDFs.
- 2026-08-18: Onboarding Stage 3 "Mail Received Date" field and its input removed from UI and the unused `document_mail_received_at` column dropped from `hr_employee_onboarding`.
- 2026-08-17: Compliance Management Phase 1 shipped — Pending Approvals, Lien Cases, and Account Status mounted; status vocabulary unified (OPEN/UNDER_INVESTIGATION/PENDING_APPROVAL/RESOLVED/CLOSED); bank_case↔account_investigation sync trigger added; RLS tightened to permission-checked policies; compliance_approve permission added.
- 2026-08-14: Data Health now excludes anonymized ERP deletion tombstones at the database-view source; deleted users remain only for immutable audit attribution and no longer appear as actionable account issues.
- 2026-08-14: ERP deletion v5 preserves immutable bank-ledger creator references through inactive audit tombstones while removing roles, personal login data, and Auth access; append-only history remains hash-valid.
- 2026-08-12: RazorpayX dismissal drift repaired — live `is_active=false` is now authoritative inactive parity, persisted immediately to the employee map, and omitted dismissal dates no longer keep verified separations open.
- 2026-08-02: RazorpayX inactive-employee payroll pause handling repaired — Payroll Inputs now surfaces live inactive status and disables Do-Not-Pay; the proxy returns the expected API limitation as a handled business response instead of an HTTP 409 runtime error.
- 2026-08-02: RazorpayX Do-Not-Pay contract corrected from official Postman evidence — proxy now translates employee ID to email and sends `{email, payroll-month, value}`, with immediate payroll read-back verification.
- 2026-08-02: July LOP push completed and live-verified in RazorpayX — add-deduction corrected to email + singular deduction-amount; all 9 employees / ₹12,658 read back exactly and temporary probe removed.
- 2026-08-02: Data Health lifecycle cleanup shipped — live RazorpayX inactive/dismissed state now auto-closes stale employment/dismissal push-failure bundles; only Active / dismissed remains when HRMS is still active.
- 2026-08-02: RazorpayX designation push root cause fixed — Data Health used linked positions.title while push/read-back verification used legacy work_info.job_role, producing false green verification without changing the displayed designation; all paths now use position title first and legacy role only as fallback.
# STATE LOG

Living cross-agent log. **Newest entry at top.** One dated line per event. Prune entries older than 30 days.

Append here whenever the owner commands a significant state change (data wipes, resets, roster changes, config/mode switches, era boundaries) or when a multi-slice arc completes. Both the in-chat build agent and Claude (external verification agent) write to this file; commit messages carry the technical detail.

---

- 2026-09-01: Attendance calendar missing-date root cause repaired — calendar range RPC now returns explicit holiday/no-data dates, and absent marking reconciles a rolling 7-day closed window so missed cron runs self-heal without overwriting leave, weekly offs, holidays, or meaningful attendance.
- 2026-08-31: Auto-pricing safety ceilings made truthful — when a configured asset limit prevents countering the selected merchant, the engine now preserves the ad, records `safety_limit_blocked`, and raises an operator alert instead of logging a misleading capped application.
- 2026-08-31: Binance bulk max-quantity replenishment repaired — unchanged totals are safely nudged below and restored to the saved ceiling, with fresh Binance detail read-back required before success.
- 2026-08-31: Binance ad-capacity calibration authentication hardened — client now sends the live access token explicitly, both functions use in-code JWT validation with direct Super Admin role resolution, and deployed gateway/CORS paths were verified.
- 2026-08-31: Binance ad-capacity calibration repaired — Super Admin authorization bypass aligned with ERP permissions, Edge Functions shortened below gateway timeout, and calibration now reports per-combination progress and exact row-level failures.
- 2026-08-30: Terminal auto-pricing authorization repaired — all internal `binance-ads` calls now use service authentication; live verification recorded six applied ratios after prior Unauthorized failures, with the circuit breaker CLOSED and human-readable log reasons visible.
- 2026-08-25: Super Admin UI access repaired — frontend permission caches/gates/sidebar/Terminal auth now treat Admin/Super Admin as full-access bypasses, matching database `admin_access`/`super_admin_access` semantics.
- 2026-08-25: ERP permission library Phase 4 shipped — Terminal permissions moved into a shared catalog for auth/role-editor/verification parity while Terminal database grants and role behaviour remain unchanged.
- 2026-08-25: ERP permission library hygiene completed — permission drift checker now validates ERP/Terminal source usages; catalogs verified against live `app_permission` and `terminal_permission` enums with no missing enum-backed keys.
- 2026-08-25: ERP permission library Phase 3 shipped — split Finance/BAMS/Clients-KYC/HRMS permissions backfilled onto existing roles only; HRMS sub-routes/sidebar and KYC approval actions now enforce granular gates while umbrella aliases preserve legacy access.
- 2026-08-10: Biometric health false alarm repaired — visitor PIN 100 and eSSL protocol-noise PIN 0 are excluded from mapping/rejection health; false device counters cleared and both devices verified with zero actionable unmapped users.
- 2026-08-05: Employee Work Information saves repaired — shift sync now ignores unchanged shifts, replaces same-day corrections, and closes prior assignments on the preceding day to prevent inclusive-date overlap failures.
- 2026-08-02: Payroll Inputs period handling repaired — UI now queries/writes canonical first-of-month dates while sending YYYY-MM to RazorpayX, making the 9 persisted July auto-LOP rows visible and pushable.
- 2026-08-02: Data Health Razorpay push closure hardened — live read-backs now persist mapped snapshots and cards resolve only after a forced employee-scoped drift rescan confirms parity.
- 2026-08-02: RazorpayX joining-date write repaired — people:edit now sends live-validated `hiring-date` in DD/MM/YYYY instead of ignored aliases; Himanshu Rajak ID 20 live read-back confirmed 11/03/2026.
- 2026-08-01: Owner-directed July attendance normalization completed — all 38 active employees marked Present for all 27 non-Sundays in both `hr_attendance` (Calendar source) and `hr_attendance_daily`; owner will manually mark actual absences.
- 2026-07-29: RazorpayX onboarding CTC tally repaired — Stage 5 no longer treats stale payroll:view-payroll amounts as live master CTC after a successful people:set-salary push.
- 2026-07-27: ERP user deletion cleanup RPC repaired — stale hard-coded token/table/column references replaced with typed schema-aware cleanup; historical records preserved while deleted user links are unlinked/anonymized.
- 2026-07-26: R10/R11/R13#1 arc — ESS Payslips tab retargeted to canonical `hr_payslips_v` + RazorpayPayslipLink deep-link (no fake PDFs); recruitment `hired` transition now auto-provisions a draft `hr_employee_onboarding` row (idempotent by candidate_id); `docs/RAZORPAYX_LIVE_STATE.md` captures post-July-25 envelope reality.
- 2026-07-25: RazorpayX one-time payout payload repair — bonus additions now send direct `data` to payroll:add-additions and proxy validates required fields before Opfin call.
- 2026-07-25: RazorpayX one-time payout gate repair — Step G payroll envelope now auto-verifies with E/F so bonus additions can retry; stale gate-lock errors show as retryable, not Razorpay rejection.
- 2026-07-25: RazorpayX salary read-back extractor fixed — verifier now consumes the proxy's live `annual_ctc` read-back, so Shubham Singh's ₹9,12,000 CTC can verify instead of showing blank.
- 2026-07-25: RazorpayX salary push finalization tightened — accepted/read-back-pending no longer counts as done; CTC revisions are complete only after live RazorpayX read-back verifies the expected CTC.
- 2026-07-25: RazorpayX salary push status split clarified — when people:set-salary accepts the exact HRMS CTC but Opfin cannot echo current CTC until payroll execution, HRMS now records "accepted/read-back pending" instead of false failure/not-synced.
- 2026-07-25: RazorpayX salary CTC mismatch repaired at source — HRMS active salary structures now auto-reconcile to employee total_salary before every salary push; Shubham Singh structure corrected from ₹9,01,800 to ₹9,12,000.
- 2026-07-25: RazorpayX salary retry repaired — proxy now sends flat people:set-salary payload with data-level custom-salary-structure/annual-ctc; retry failures open/readable verification dialog instead of silent `[object Object]` toasts.
- 2026-07-25: RazorpayX salary revision green-badge logic hardened — CTC sync now requires live people:view salary read-back with matching Annual CTC; stale payroll-view / HTTP-only success logs no longer count as synced.
- 2026-07-22: Buyer approval root-cause hardening — approvals now update the exact `resolved_client_id` terminal stub, name-based sibling auto-approval removed, pending approval uniqueness guards added for sales_order/userNo/resolved client, and phone uniqueness now normalizes digits.
- 2026-07-22: Buyer approval duplicate-stub loop repaired — terminal buyer creation now reuses pending stubs by Binance userNo/phone, persists userNo during creation, and the Dakoriya duplicate pending client rows were consolidated for approval retry.
- 2026-07-22: Biometric eSSL linking repaired — HR/admin mapping policy restored, v4 attendance status validators aligned (`incomplete`/`no_data`), PIN→Badge auto-link triggers added, and existing matched PINs replayed parked punches into attendance.
- 2026-07-21: RazorpayX Stage 5 write-back patched — HRMS-wins now sends raw source values, avoids misusing API routing `employee-type` for employment category, uses documented `bank-ifsc`, and records read-back-unconfirmed Razorpay fields instead of looping Finalize on fields people:view does not echo.
- 2026-07-21: RazorpayX onboarding Stage 5 hardening — already-linked employees now require/auto-refresh a real reconciliation tally before Finalize; choices persist synchronously through finalize; post-finalize verification fails on actual remaining mismatches, not missing override state.
- 2026-07-20: RazorpayX onboarding Stage 5 flow corrected — create-in-RazorpayX invite step added, unverified stale ESSL badge IDs no longer hydrate, ESSL writes only unlock after verified RazorpayX Employee ID, and HRMS-wins reconciliation pushes HRMS data back to RazorpayX on finalize.
- 2026-07-19: RazorpayX onboarding create path re-aligned to live Opfin behavior — people:create now includes required hire_date/full profile minus Employee ID, then retries email-keyed attach/read-back automatically to avoid manual finalize loops.
- 2026-07-19: RazorpayX onboarding ghost-email behavior identified — `artwithakriti2529@gmail.com` returns email-exists but API edit/view cannot locate it; HRMS now stops downstream ERP/local activation on Razorpay failure and requires email purge/change before retry.
- 2026-07-19: Shubhams Singh stuck onboarding reservation ID 75 cleared after deleted/partial Razorpay record; linked HR badge moved to PENDING-8A047792, pending eSSL create commands cancelled, delete commands queued, next unified RazorpayX/ESSL ID is 76.
- 2026-07-19: RazorpayX onboarding create contract corrected per official Postman collection — people:create now sends only email/name/type, all Employee ID/PAN/bank/CTC enrichment uses documented email-keyed people:edit/set-salary, and off-contract people-id attach recovery was retired.
- 2026-07-19: RazorpayX onboarding proxy redeployed after removing a stale unreachable enrichment path and restoring attendance push lock checks; Stage 5 create_person now routes fresh/create/recovered records through the single people-id-aware enrichment helper.
- 2026-07-19: RazorpayX onboarding partial-create root cause fixed — create_person now enriches mapped/recovered/fresh employees with ERP PAN/bank/CTC before returning success; incomplete enrichment blocks Stage 5 completion instead of silently leaving partial Razorpay records.
- 2026-07-19: RazorpayX onboarding create/reset repair shipped — proxy now attempts automated email-keyed Employee ID attachment instead of asking operators for people-id; stuck Shubhams Singh local reservation 73 cleared, HR/ERP badges reset, and old eSSL PIN delete queued so Stage 5 can reserve fresh ID 74.
- 2026-07-19: RazorpayX onboarding email-exists recovery improved — when Opfin exposes the internal people-id, the proxy now auto-attaches HRMS reserved Employee ID and repairs the map, avoiding manual Stage 5 people-id entry.
- 2026-07-19: RazorpayX onboarding stale reservation reset hardened — local ID 72 was cleared after operator deleted the partial Razorpay employee; allocator now returns 73 and Stage 5 reset calls service-role proxy to clear onboarding/hr_employee/user badges, cancel pending eSSL identity commands, and avoid reusing burned IDs.
- 2026-07-19: RazorpayX onboarding email-exists path converted from HTTP 409 runtime failure to `ok:false` business response; Stage 5 now offers operator-assisted verified linking by Razorpay employee-id when Opfin cannot expose direct email lookup.
- 2026-07-19: RazorpayX onboarding finalize conflict handling hardened — Opfin code 7/email-exists now attempts exact-email recovery and repairs hr_razorpay_employee_map; Stage 5 now surfaces parsed business errors instead of raw Edge 502 runtime text.
- 2026-07-19: Onboarding finalize retry idempotency shipped — existing ERP users and existing/mapped RazorpayX employees are now reused/repaired and linked back to hr_employees instead of blocking Stage 5 completion after partial prior attempts.
- 2026-07-19: Onboarding finalization retry hardening shipped — RazorpayX create_person now uses official people:create payload/casing instead of rejected people:add; create-erp-user now reuses orphan Supabase Auth identities on retry instead of failing "email already registered".
- 2026-07-19: HRMS onboarding persistence hardening shipped — Stage 1/2 autosave, Stage 3/4 checklist/date autosave, Stage 5 autosave + saved `bank_details`; safe update merge prevents partial stage saves from nulling existing fields such as date_of_joining, reporting_manager_id, ERP role, documents, and reserved RazorpayX/ESSL ID.
- 2026-07-19: Payroll doctrine v2 — Stages 1–3 shipped. `fn_generate_payroll` + `apply_due_scheduled_salary_revisions` retired (both raise). New isolated Shadow Payroll namespace (`hr_shadow_payroll_runs`/`_lines`/`_component_breakdown`) + `compute-shadow-payroll` edge fn + `/hrms/payroll/shadow-calculator` "Payroll Calculation (Building)" page compute PF/ESI/PT/TDS/LOP locally using the Razorpay compliance mirror and diff line-by-line against imported Razorpay payslips. `hr_drift_alerts.resolution_direction` added (update_hrms | push_to_razorpay | update_essl | dismissed) with `hr_drift_open` view rebuilt around it — bidirectional resolution ledger for Stage 3 Data Health surface. Stages 4–6 (per-employee structure push, bulk CSV structure importer, unified profile history) queued.


- 2026-07-18: RazorpayX payroll history restore verified after redeploy — hr_razorpay_payslip_records=61 and reflected hr_payslips(source=razorpay_import)=61 across 16 employees, spanning 2024-07 to 2026-03.
- 2026-07-18: RazorpayX payroll history restored to API-first model — `payroll:view-payroll` is the canonical Opfin source for monthly payroll amounts/components; no payslip PDF/download endpoint exists in official surface, so PDFs are dashboard-only.
- 2026-07-18: v4 attendance engine — Phase 0 schema deployed (sessions table, engine settings, punch classification columns); Phases 1–2.5 code deployed but engine unhydrated (punches wiped); Phases 3–7 parked pending Monday's real punches.
- 2026-07-18: Razorpay last-mile — Slices 4 (F&F dismiss with CONFIRM_DISMISS ack) and 5 (attendance verify affordance on Period Lock page) shipped; sandbox host documented in RAZORPAYX_COMMISSIONING.md with manual rehearsal path. Deferred queue: Payroll Adjustments hub, Advance Salary flow (needs hr_loans.razorpay_advance_id), Contractor locked-state polish, in-app sandbox toggle.
- 2026-07-18: Roster changed to 37 employees total (22 active + 15 soft-deleted; was 39). Salary structures = 112 rows (~5 components/active-emp — deliberate per-component shape). Biometric device users unmatched = 21 (17 ghost enrollments with no hr_employees row + 4 dismissed employees; all 22 active fully linked on both devices).
- 2026-07-18: Owner wiped ALL attendance punches / sessions / daily rows deliberately — clean attendance-logic observation starts Monday 2026-07-20. Zero rows in those tables are intentional.
- 2026-07-18: Full-platform functionality audit — E1 apply_error root cause identified (hr_razorpay_employee_map dual-unique-constraint upsert) and fixed inline in upsertMap; E2 payslip visibility traced to split destination (hr_payslips=31 vs hr_razorpay_payslip_records=11) + zero pdf_url capture; E3 pull_person 389/48h confirmed as scheduler cadence (auto-sync cron), healthy.
- 2026-07-18: RazorpayX payslip history was showing pre-execution setup (gross==net, deductions=0) because operator had verified `payroll:view-payroll` as the payslip envelope. Purged hr_razorpay_payslip_records + hr_payslips (source='razorpay_import'), un-verified the payslip envelope, and gated import_payslip_history_range behind envelope verification. Owner-facing panel now shows explicit "finalized payslips unavailable" state.

- 2026-07-19: RazorpayX payslip pull hardened — added `bulk_applied|locked|recalled` run-status gate + `pull_payslips_endpoint_verified` gate to `pull_payslips_for_period`; `import_payslip_history_range` now skips (does not silently pull) months without a finalised RazorpayX run. Purged all `razorpay_import` rows from `hr_payslips` (all 87) + all `hr_razorpay_payslip_records` — every purged row was a CTC/12 default because `hr_razorpay_payroll_runs` is empty (no run has been executed on the RazorpayX side yet). Payslip PDFs remain dashboard-only: Opfin API has no PDF/download endpoint (verified in prior audit); when RazorpayX includes a `pdf-url` field in `view-payroll` response for a finalised month, we already persist and render it.

- 2026-07-19: Attendance v4 engine hardened per A1/A2 dispositions. A1: zero-activity days write status=`no_data` (not `absent`) and are skipped from the legacy `hr_attendance` mirror — Phase 3 remains sole owner of absence marking. A2: OUT arriving > watchdog_hours (default 14) after an open IN is now classified `orphan_out` and does NOT close the session, preventing 20h+ monster sessions from forgotten punch-outs. A3 verified: `hr_v4_resolve_shift` returns NULL for all 22 unassigned employees, `hr_v4_shift_metrics` short-circuits to zero — no phantom late/early/OT tomorrow.

- 2026-07-19: Payslip import gate fixed. Added `discover_and_seed_runs` proxy action that probes RazorpayX view-payroll per-month with a pilot employee and auto-seeds `hr_razorpay_payroll_runs` (status=bulk_applied) for months detected as executed (multi-signal heuristic: deductions>0, statutory PF/ESI/PT>0, TDS>0, real payslip-id, PDF url, or net<gross). Auto-verifies payslip envelope on first 2xx. UI: Payslip History Import page now has Step 1 Discover / Step 2 Import buttons with an optional pilot rpId input. June 2026 salary register CSV supplied by owner for reference-only tally (no data entry).
- 2026-07-19: Retired in-house payroll engine (fn_generate_payroll now raises). RazorpayX is the payroll authority. Added reg_* statutory-split columns to hr_razorpay_payslip_records and a CSV Salary Register uploader at /hrms/payroll/salary-register-import to close the PF/ESI/PT/TDS/employer-contribution API gap. Staging tables hr_payroll_input_additions and hr_payroll_input_deductions created for future push-side flows (approved-revision, LOP, one-off deductions).
- 2026-07-19: DOCTRINE — RazorpayX is now the PRIMARY payroll authority; HRMS is its image/feeder. Every payroll figure UI-tagged (razorpay / register_csv / dashboard_only / local_estimate). `hr_employee_salary_structures` locked as read-cache (authenticated writes blocked, service_role only via push→refetch). New primary alarm: `hr_razorpay_payroll_freshness` view (freshness + CSV state), replacing math-vs-math drift as the health signal. Attendance engine v4 stays local (input feeder). See docs/PAYROLL_DOCTRINE.md.
- 2026-07-19: Shadow payroll engine v2 — verified against 10 real payslips. Added per-employee `pf_enabled`/`esi_enabled`/`pt_enabled` + `custom_structure_pct` on hr_employees. Fixed EDLI (0.5%→1% flat), added Employer-PF earnings-side line (13% × min(Basic,15000)), implemented per-component LOP+KPI shrink. Backfilled enrollment for Priya/Amit/Lavany/Khushbu/Uday/Vicky. LWF explicitly locked OFF (owner directive).
- 2026-07-25: A6-A11 groundwork shipped. Slice 5 live: hr_emit_notification + hr_broadcast_notification_to_hr helpers; auto-notify triggers on hr_leave_requests, hr_announcements, hr_razorpay_payroll_runs, hr_salary_revisions. Slice 1 live (backend): canonical view hr_payslips_v (security_invoker, RazorpayX-sourced) + hr_payslip_link_orphans() audit. Slice 2 backend: advance_type/repayment_source on hr_loans + hr_create_salary_advance RPC (UI hookup on LoansPage still pending). Slice 4: fix_proxy.py → .archive/scripts/, docs/binance_api-17.pdf → docs/reference/binance/, docs/REPO_LAYOUT.md added; apply-scheduled-salary-revisions confirmed already removed. Slice 6 backend: sandbox_mode/sandbox_base_url/sandbox_revoke_after on hr_razorpay_settings (proxy routing + UI still pending). Slice 3 (regularization repurpose) still pending. Data Health email-dispatch tile still pending.
- 2026-07-26: HRMS Slices A–E shipped: hr_payslips_v canonical view + orphan RPC + Data Health parity tile; NewSalaryAdvanceDialog + hr_create_salary_advance/ack RPCs wired into LoansPage; AttendanceRegularizationPage repurposed to Interventions (reason_code + mandatory audit log); SandboxToggleCard on RazorpaySyncPage with hourly hr_razorpay_sandbox_auto_revoke() cron and proxy runtime BASE override; Email dispatch health tile on Data Health.
- 2026-07-26: Slices R1–R5 shipped. `pushWithVerification` unifies every RazorpayX write client-side; `hr_lop_days` + `hr_stale_session_held` become canonical LOP; Attendance Watchdog folded into `/hrms/attendance/regularization` (alias `/attendance/watchdog`) with three verbs (set out-time / confirm long shift / void) + mandatory reason codes; Day drill-down renders kept/suppressed punches, arithmetic session labels, night-span/shift-deviation flags, and per-day LOP contribution; `hr_roster_completeness` powers new Data Health tile.

- 2026-07-26: F8/F9/F10 shipped. Cockpit drift-review now counts only unexplained (auto_status=open) drift — ±₹5 & TDS rounding auto-tolerated. Email dispatcher retry cron scheduled every 5m with exponential backoff (15m/45m/120m) → dead-letter escalates to drift alert. System Pulse gained tiles for unexplained drift, email dead-letter/ghost residual, auto-absent marker age. Data Health gained "Unexplained only" filter and ghost-email residual card. Deno test seeded for auto-absent v4 window semantics.

- 2026-07-26: Wave 3 foundation — added round-trip receipt columns (attendance_readback_* on hr_razorpay_payroll_runs; readback_* on hr_payroll_input_additions/deductions); new hr_device_roster_reconciliation_log + latest-view. Shipped W3 (hr-essl-sync-devices does three-way parity → auto-deletes dismissed enrollments, auto-re-enrolls missing actives, flags ghosts/PIN-mismatch), F5 daily SET TIME command in biometric-refresh-scheduler, and Device roster parity tile on System Pulse. F2 auto-resolve confirmed already live in hr_watchdog_open_sessions. W1 (attendance-fetch read-back) and W2 (view-payroll read-back) proxy wiring, F9 ghost-email auto-remediation, F10 test wire-up remain queued.
- 2026-08-01: Monthly Business Report added (report_email_configs, profit variant, 1st of month 11:30 IST); dispatcher gated to day-1 for monthly configs; daily-report-email .in() filters chunked (fixed 431 on month ranges); July 2026 report manually sent to both recipients.
- 2026-08-01: Weekly-off integrity — deleted all 'absent' rows falling on employees' weekly-off days (July 19 & 26 Sundays, 47 legacy rows) and reset matching hr_attendance_daily rows to no_data; added trg_hr_attendance(_daily)_block_absent_weekly_off guards; calendar UI now renders weekly-off over absent.
- 2026-08-02: Roster drift added — RazorpayX-only people (active in RazorpayX, no HRMS employee) now tracked in hr_razorpay_orphans via proxy action scan_orphans + hr-razorpay-orphan-scan (daily 06:30 IST cron) and surfaced on /hrms/data-health. First scan: 42 people, 34 matched, 8 dismissed, 0 orphans.
- 2026-08-02: Root-caused RazorpayX dismissal failures (Sneha Shukla, id 42). people:dismiss resolves the person via their RazorpayX *user login*; employees who never activated their RazorpayX account (is_active=false) return code 8 "Unable to locate the user" and CANNOT be dismissed via API — dismiss them in the RazorpayX dashboard. Proxy now pre-flights people:view and returns RZP_ALREADY_DISMISSED (treated as verified success) or RZP_NOT_ACTIVATED (manual_required + drift alert) instead of an opaque 502.
- 2026-08-02: Attendance Summary repointed to canonical hr_attendance_month_summary RPC (biometric-evidence, same source as payroll LOP); legacy hr_attendance now only surfaces as a 'manually marked' reconciliation warning.
- 2026-08-02: Data Health gained the reverse direction — "Pull ← Razorpay" (hr-razorpay-pull-apply) adopts RazorpayX values into HRMS with live re-read, sensitive-field gate, pushback audit and per-employee re-scan; drift scan PAN bug fixed (was read from work_info, lives on hr_employees).
- 2026-08-02: CURRENCY BUG root-caused — one-time payouts (salary-revision bonuses) were pushed to RazorpayX as amount*100. Shubham Singh's ₹5,000 Jul-2026 bonus landed as ₹5,00,000 (Opfin payroll:add-additions is RUPEE-denominated; same echo returns salary:76000). Fixed src/lib/oneTimePayoutPush.ts to send rupees and added echo verification against the returned additions map. The already-staged ₹5,00,000 addition must be reset in RazorpayX for employee-id 70 / 2026-07 and re-pushed before the July run executes.
- 2026-08-02: Shubham Singh July bonus re-pushed to RazorpayX at correct ₹5,000 (prior ₹5,00,000 entry reset via payroll:reset-modifications; echo-verified via payroll:view-payroll). Root cause 2: Opfin add-additions/add-deduction requires an ARRAY of {label,...} objects — the keyed-map wire shape returns HTTP 500 UNKNOWN_EXCEPTION; razorpay-payroll-proxy now sends the array form.
- 2026-08-02: Payroll LOP authority corrected to maintained Attendance Summary (`hr_attendance` via `hr_attendance_month_summary`); Auto-LOP now calls that exact RPC instead of biometric-derived `hr_compute_lop_days`.
- 2026-08-02: Shubham Singh July bonus re-staged on RazorpayX. Root cause of "not visible": the addition head was pushed with label "Ad" (raw revision_reason), an unreadable 2-char head on the run. Fixed oneTimePayoutPush to anchor labels on payout type ("Bonus — <reason>"); reset-modifications + re-push as "Bonus" Rs 5,000, echo-verified live via payroll:view-payroll.
- 2026-08-02: Data Health false "active in Razorpay" root-caused: drift scan compared stale cached RazorpayX snapshots (nightly refresher was 401-ing on anon key, sent nested payload, and skipped dismissed people). Fixed: proxy read_person_by_id accepts flat/nested body + allow_dismissed; drift-scan re-pulls snapshots older than 12h inline and never asserts active_state from an unrefreshed snapshot; not-found in Opfin => inactive. Full re-scan run; IDs 9/77 now correctly inactive.
- 2026-08-02: Loans & Advances integrity — hr_payroll_auto_recoveries view (loan EMIs + deposit/error-recovery installments) surfaced read-only on Payroll Inputs deductions tab; hr_close_loan + hr_record_manual_loan_repayment RPCs; loan lifecycle now supports paused (scheduler skips); EMI x tenure validation and disbursement stamped at approval. Self-tested end to end.
- 2026-08-02: July 2026 payroll reset before re-run — cockpit now has 10 steps (new Step 3 "Salary revisions"); all 9 pushed LOP deductions removed from RazorpayX via payroll:reset-modifications (Opfin rejects zero-amount add-deduction, code 41 — there is no delete verb) and the local LOP rows deleted; all 19 July additions (Performance bonus) cleared in RazorpayX and flipped back to un-pushed locally (rows kept, status "Push again"). Echo-verified via payroll:view-payroll per employee. Note: employee rp#10 retains 5 pre-existing reimbursement-type additions (Legal fees etc.) not staged from HRMS — reset-modifications does not clear type-4 reimbursements.
- 2026-08-02: LOP/shadow monthly base ladder fixed — RazorpayX annual CTC (hr_employees.total_salary) now outranks the mirrored component sum, which was overstating bases (Archita 11,508→11,000; Poonam 15,638→15,000). Mirror is fallback-only.
- 2026-08-04: One-time payout parity shipped — register pay heads classified into hr_payslip_pay_head_lines, hr_reconcile_one_time_payouts + backfill RPCs wired into salary register import, payslip email/ESS profile now split regular gross vs one-time recovery, revision dialog remark becomes the pay-head name.
- 2026-08-04: Added READ-ONLY pilot report "Salary Register (In Progress)" (/hrms/reports/salary-register-projection) backed by new select-only edge fn hr-projected-salary-register — projects the RazorpayX salary-register layout from HRMS data (CTC-inclusive doctrine, hr_compute_lop_days, staged inputs, auto-recoveries, one-time payouts split payroll vs outside_payroll). Writes nothing.
- 2026-08-04: Salary-structure mirror re-anchored to RazorpayX CTC — RazorpayX API exposes CTC only (no component split), local seed was adding statutory on top (Tomar 12,01,800 vs 12,00,000). Added hr_align_salary_structures_to_razorpay() + trigger on razorpay pull; realigned 25 employees, drift now 0/35.
- 2026-08-07: Root cause found for missing 2–6 Aug daily P&L: 9 pg_cron jobs were owned by supabase_read_only_user and had been failing with 'permission denied for function net.http_post' since 3 Aug. Re-scheduled all under postgres (old broken duplicates remain but only error, no double execution) and backfilled daily_gross_profit_history for 02–06 Aug plus today's asset-value snapshot.
- 2026-08-09: Ran one-time RazorpayX identity backfill (proxy action `backfill_identity_all`) for all 42 mapped employees — re-pushed department, designation, gender, DOB, phone and state=Madhya Pradesh via people:edit. 35 applied, 7 failed with code 8 (all inactive/ex-employees, expected). people:view does not expose gender/state, so those two are unverifiable via API.
- 2026-08-10: Attendance regularization approval flow shipped — employee raises in ERP, HR reviews first, optional push to reporting manager (decides in ERP), HR finalizes; regularization-approval email template + manager routing trigger on hr_attendance_regularization_requests.
- 2026-08-10: Leave + regularization approval flows hardened — dropped blanket authenticated_all_hr_leave_requests policy, added hr_is_hr_staff() (role is named 'HR Manager', not 'hr'), field guards blocking employee self-approval and manager edits, unique open regularization per employee/date, removed duplicate leave decision notify (dead /employee/leaves link), added regularization in-app notifications, employees can withdraw forwarded regularizations.
- 2026-08-11: HR Manager RLS lockout fixed system-wide — 18 policies across 15 hr_* tables gated on has_role(uid,'hr'), a role that does not exist (real name is 'HR Manager'), so HR Managers fell through to employee-self rules and saw only their own row (symptom: empty employee list on /hrms/separation). All such policies rewritten to public.hr_is_hr_staff(auth.uid()) via ALTER POLICY; Super Admin/Admin branches, command scope and employee-self rules preserved. Verified: 0 policies retain the 'hr' literal; hr_is_hr_staff true for the HR Manager account.
- 2026-08-11: Merged HRMS "Hour Accounts" + "Monthly Hours" into a single /hrms/attendance/hours page (Hours & Overtime). New view hr_monthly_hours_unified (hr_hour_accounts + hr_attendance_daily) is now the single truth; legacy hr_monthly_hours_summary (built on stale hr_attendance) no longer powers any page. Old routes redirect.
- 2026-08-12: Attendance Calendar migrated to v4 engine (hr_attendance_day_range via useAttendanceDayRange); added hover day tooltip + click-through AttendanceDayDialog; HoursOverview drilldown rerouted through the sanctioned reader. Remaining V1-guard violations (ReportsPage, AttendanceSummaryPage) are pre-existing aggregate readers.
- 2026-08-12: Attendance v4 recompute made absent-preserving (zero-punch rebuild no longer reverts absent/leave/week-off to no_data); added hr_heal_no_data_absences() and healed 31 historical rows from 2026-07-17 onward. Sundays correctly remain no_data (weekly off).
- 2026-08-12: Attendance made shift-aware — new `hr_v4_detect_shift` judges each day by the shift ACTUALLY worked (best-fit on first-in, only on clear mismatch beyond assigned grace + `shift_match_tolerance_hours`). Wired into `hr_v4_recompute_range` (late/early/OT/register/mirror), `auto_track_late_early`, `hr_resolve_stale_session` (mark_shift_end) and `hr_attendance_day_detail`. Backfilled 2026-07-17→today: 60 days re-scored as off-shift, 468 unchanged.
- 2026-08-12: F&F is now the single settlement engine for leavers — autofill pulls live loans (full outstanding), unapplied penalties (penalty_amount), refundable deposits (security + recovered error-recovery); approval pushes two consolidated lines to the RazorpayX final month via hr-push-fnf (read-back verified); Mark-Paid runs hr_close_fnf_sources; hr-schedule-deposits skips leavers from their LWD month onward.
- 2026-08-13: Corrected RazorpayX `people:dismiss` to the published Postman contract `{email,dateOfDismissal}` (camel-case date; removed unsupported employee/type/name/reason fields) and wired Data Health inactive-state drift to the dismissal action.
- 2026-08-13: Data Health full-scan timeout root-caused to serial Razorpay snapshot refreshes plus serial per-field reconciliation; `hr-drift-scan` now uses bounded worker pools for both phases and was live-verified scanning 44 employees with HTTP 200 and zero refresh failures.
- 2026-08-12: Drift scan now force-refreshes RazorpayX snapshots for HRMS-inactive employees and anyone with an open active_state/dismissal_state alert (stale-cache was keeping dismissed people flagged 'active'); Data Health exposes a confirmed 'Remove from eSSL device' action for inactive staff — safe because punches persist in hr_attendance_punches keyed to the employee, not the device roster.
- 2026-08-14: ERP permanent user deletion fixed at root — `delete_user_with_cleanup` now discovers every live FK to `public.users`, anonymizes nullable historical references and removes mandatory/cascade dependents; verified with a linked `erp_product_conversions.created_by` row and cleaned the test data.
- 2026-08-13: User deletion made auth-aware — delete_user_with_cleanup v4 now clears FKs to auth.users too; purged 5 orphaned auth logins (muskan, neha, sneha, vandana, akriti) that survived earlier ERP deletions.
- 2026-08-13: Root-caused HRMS 'only 1 employee' — users.role_id (set by User Management) was never mirrored into user_roles, which has_role()/hr_is_hr_staff() read, so the Super Admin owner fell back to the 'own record' RLS policy. Added sync_user_role_id_to_user_roles trigger + backfilled missing role links (29->30).
- 2026-08-14: ERP deletion authorization moved into delete-erp-user after validated JWT identity; privileged cleanup now uses a fresh service client, removing unreliable browser-JWT propagation while preserving Super Admin/Admin/granular permission checks.
- 2026-08-14: Standby role locked down — ERP routes/sidebar/mobile nav blocked, permissions forced empty; only /profile (attendance, payroll, payslips) is reachable.
- 2026-08-14: Comp-off balances rebuilt from the hr_compoff_credits ledger (310 phantom CO days → 33 actual). Root cause: CompOffPage manual "Allocate"/"Allocate All" buttons re-added credit days on top of the automatic BEFORE INSERT trigger (fn_allocate_compoff_credit). Manual allocate path removed; page is now read-only over the ledger.
- 2026-08-15: P&L carry-forward cost basis shipped — periods/days with zero purchases now reuse the last purchase day's effective rate (ProfitLoss dashboard, GrossProfitHistoryTab live row, snapshot-daily-profit). daily_gross_profit_history gained purchase_rate_carried / purchase_rate_source_date; 2026-06-22 (stale zero-rate row) and 2026-08-15 restated.
- 2026-08-15: Company-wise Balance Sheet shipped (Financials > Reports). Ledger-derived per subsidiary via fin_entity_* views/functions; transfers now classified intra/inter-entity/unlinked; no plug entries — residual disclosed as 'Opening position with no ledger history'. PDF+Excel export.
- 2026-08-15: Balance Sheet Phase 4/5 shipped — valuation-basis selector (Cost/Market/LCOM), wallet→company mapping admin (fin_wallet_entity_map only, wallets untouched), MCA G.S.R. 207(E) crypto note for private limited entities, DRAFT—FAILED VERIFICATION watermark + checksum on PDF/Excel, generation logged to fin_balance_sheet_generation_log.
- 2026-08-15: Balance sheet v4 defect fix — fin_intercompany_v now excludes reversed legs symmetrically (₹24,99,998 overstatement removed; Shubham receivable 1,63,33,603.91), INTERCOMPANY_REVERSAL_LEAK regression check added, share_capital emitted as NULL/"NOT AVAILABLE" for private limited entities, export path exercised end-to-end (PDF+XLSX with DRAFT watermark, checksum D30EC0D9F368271B, generation-log row written), H2 round-trip PASS on all 20 anchored accounts.
- 2026-08-16: Regularization + leave notification emails moved off task@ transactional templates to new hr-workflow-notify edge fn sending from hr@blynkex.com with HR header/signature branding; sample verified to shubham.singh@blynkex.com.
- 2026-08-17: Compliance Phases 2–5 frontend shipped — Command Centre overview tab, Regulatory tab (NCRP/cyber-cell register, STR maker-checker register, statutory calendar), Governance tab (audit trail, credential access log, config vocabularies), case timeline composer on bank cases, account exposure badges; compliance-reminders edge fn deployed (dry-run OK: 3 recipients, 2 items) and cron scheduled 09:00 IST daily plus document-status recompute 08:50 IST.
- 2026-08-17: Compliance Phases 1-5 recheck completed — all tables/RLS/grants/triggers/view/RPC/crons verified present; compliance_approve permission was granted to zero roles (approvals workflow blocked) and has now been granted to Super Admin + Admin; compliance-reminders edge fn dry-run 200 OK; typecheck clean.
- 2026-08-19: Leave Year-End Reset tab removed (page/route/sidebar deleted); `hr-year-end-leave-reset` cron unscheduled, execute_leave_reset() stubbed to no-op; all hr_leave_types set to non-expiring carry-forward. Policy: only Comp-Off is settled, via monthly payroll encashment.
- 2026-08-19: HR Document Studio P3/P4 shipped — Generate + Issued tabs live (browser print-to-PDF, frozen HTML artefacts in hr-doc-issued, BLY-000001 reference pattern); DOCX letterheads import into the native editor via mammoth; salary-bearing letters opened to all HR staff.
- 2026-08-19: Doc Studio correctness pass — placeholder kind stored explicitly (not token-spelled), CSS/script blocks excluded from placeholder scanning, LWD no longer falls back to resignation_date, letter_date/LWD/conduct editable before issue, IST letter date, tenure works for serving employees, monthly_gross renamed Monthly CTC, reference sequence now per letter type. Parked: true-PDF artefact, letterhead/logo, bulk appraisal issue, supersede lineage.
- 2026-08-19: Locked-DOCX lane hardened — real signature-image merge via docxtemplater image module ({{%TOKEN}} syntax), import-time dry-render validation, Firefox-safe downloads, draft marking in reference. Locked DOCX is the default lane for imported Word letters; HTML lane remains for letters authored inside HRMS.
- 2026-08-19: RazorpayX identity push — dropped `gender` from the people:edit payload. Opfin's people contract has no gender field (0/45 pull snapshots carry it); it accepted the key with HTTP 200 and no-oped, so every employee diffed as "changed: gender" on every run. Gender / personal phone / personal email / WhatsApp are dashboard-only in RazorpayX.
- 2026-08-19: Job Role retired as a separate field — Position is now the single designation everywhere (Add/Edit employee, profile work info, onboarding Stage 1/5, employee cards). Legacy `job_role` column is auto-mirrored from the Position title (backfilled) so RazorpayX push, documents and drift scan keep resolving a title.
- 2026-08-19: Root-caused failed ERP credential emails (send-hr-email used stale HR_SMTP_* secrets -> Gmail 535). send-hr-email now resolves SMTP from active hr_mailboxes row (HR_GMAIL_*). Re-issued temp passwords + resent credentials to Syed Adnan Imran, Urvashi Sharma, Harmeet Singh Khalsa (force password change on).
- 2026-08-19: HR email branding standardised — new wrapHrEmail() shell in _shared/hrSignature.ts (Blynk header + HR signature + confidentiality footer), enforced centrally in send-hr-email so onboarding/credential/careers mails can never go out plain. Idempotent; no mails resent.
- 2026-08-19: HR Document Studio Word→PDF now converts server-side via Adobe PDF Services (edge fn hr-doc-convert-pdf, secrets ADOBE_PDF_CLIENT_ID/SECRET/ORG_ID). Archived once as *.adobe.pdf; browser rasterisation retired for DOCX letters.
- 2026-08-20: HR doc emails — fixed stray "=20" artifacts (denomailer quoted-printable encodes trailing spaces); bodies now trailing-space-stripped. Sample emails for all 5 letter categories re-sent to shubham.singh@blynkex.com with the archived letterhead PDF attached. Reference number now injected into any Word token mapped to reference_no (e.g. {ref_no}); previously-issued letters must be re-issued to print it.
- 2026-08-19: Letter watermark grey-box fix — DOCX media now flattened onto white before issue (alpha no longer baked into an opaque rect by PDF conversion); Jatan's BLYNK/RELIEV/2026-27/0003 docx re-flattened and PDF re-converted.
- 2026-08-19: HR letter emails made mobile-responsive (fluid 640px card, <=620px media query stacking the fact table, 16px gutters, word-break on long refs/emails); all 7 HR mail edge functions redeployed; 5 samples re-sent to shubham.singh@blynkex.com.
- 2026-08-20: Security hardening — 8 document buckets (kyc-documents, employee-documents, investigation-documents, documents, sales_attachments, transaction-bills, internal-chat-files, task-attachments) switched to PRIVATE with authenticated-only storage policies; frontend now signs URLs via src/lib/storage-url.ts + global click interceptor. users.password_hash revoked from anon/authenticated. WebAuthn RPCs self-scoped. HR edge fns (essl push/clone/rekey/sync, drift-scan, snapshot-refresh, push-fnf, razorpay-pull-apply) gated — all return 401 anonymously; cron jobs migrated to x-scheduler-secret and verified 200.
- 2026-08-24: Zone-aware terminal automations shipped — shared `src/lib/adZone.ts`, Zone filter in Ad Manager, zone-grouped bulk ladder + mixed-zone warnings, `searchP2PMerchant` now zone/badge/level aware (classifies mass|profession vs block), auto-price rules gain merchant-level + min VIP targeting and an enforce_zone_match guard that skips ads outside the targeted zone (verified live: P2P ~103.5 vs Block ~100.0 INR).
- 2026-08-24: Terminal zone work Phase 3+4 complete — Zone Book dialog (side-by-side P2P/Block live books with our rank/spread) in Ad Manager; pricing logs now record ad_zone + merchant identity/VIP with a Zone filter; engine state records last_zone/last_merchant_identity; effectiveness snapshots carry dominant zone/merchant level.
- 2026-08-24: Terminal Phase 5 shipped — zone selector on ad creation (gated by Binance getAvailableAdsCategory), zone-attributed Analytics "Zones" tab via terminal_ad_zone_map, mixed-zone warning on bulk floating price.
- 2026-08-24: Terminal zone audit closed — rule dialog now shows per-ad zone pills + zone-mismatch warning; badge/zone helpers centralized in supabase/functions/_shared/adZone.ts (binance-ads + auto-price-engine redeployed); terminal_ad_zone_map execute grant verified for authenticated.
- 2026-08-25: Approved-leave days now stamped as 'on_leave' in hr_attendance_daily (trigger hr_stamp_leave_attendance + backfill of all approved leaves); previously such days stayed 'no_data' and looked blank on the calendar.
- 2026-08-25: LOP leave accounting corrected — hr_lop_days + hr_lop_days_window now count only the leave days the HR cascade actually covered (paid_days applied to real working days; uncovered days fall to LOP), and days actually worked during approved leave (hr_leave_worked_days) are no longer counted as both present and leave. Verified Aug-2026: Lokesh 2 paid/0 LOP, Uday 4 working leave days with 3 covered → 1 LOP day (was silently 0).
- 2026-08-25: HRMS de-subdivided — single hrms_view/hrms_manage (+razorpay_sync) umbrella; sub-module keys removed from catalog/routes/sidebar and deleted from role_permissions (legacy keys still alias to umbrella). Phase 5 audit: added route-level PermissionGates to 15 previously ungated ERP routes (stock, accounting, statistics, P&L, financials, compliance, leads, risk, reconciliation, tasks, user-management, erp-entry, help-assistant(+admin), ra-dashboard) matching existing sidebar gates.
- 2026-08-25: Permission overhaul closed out — server-side inheritance added (`public.permission_implications` + rewritten `has_permission`/`user_has_permission` with transitive umbrella/legacy expansion and admin_access/super_admin_access bypass, mirroring the client `expandPermissions`); client hook now honours the same admin bypass; zero-grant keys backfilled (risk_management_view/manage, support_view/manage, report_formats_manage → Super Admin/Admin, +risk/support view-manage → COO); remaining ungated routes gated (/utility, /utility/invoice-creator, /utility/payment-screenshot → utility_view|manage; /settings/exchange-accounts → terminal_manage); user_activity_log blanket `true` ALL policy dropped (self-or-user_management_manage read, self insert). Audit: all 67 ERP catalog keys exist in app_permission, all 63 terminal keys held by ≥1 role, catalog drift check + build clean.
- 2026-08-26: Approved attendance regularizations are now authoritative for the whole day — device punches on a regularized window are set aside (except the approved in/out themselves), and worked hours/late/early-out/OT recompute from approved times. Backfilled all 16 approved regularized days.
- 2026-08-26: HRMS unified Requests inbox added at /hrms/requests (registry-driven: leave + attendance regularization, read-only aggregation over source tables). HR leave/regularization notifications now deep-link there with the request id; dedicated Leave Requests and Regularization pages remain the approval owners.
- 2026-08-29: Grace period centralised — per-shift grace removed from UI and ignored by hr_v4_shift_metrics / hr_v4_detect_shift / auto_track_late_early / biometric-webhook; new hr_effective_grace_minutes() reads the Default Attendance Policy only. Per-shift break duration unchanged.
- 2026-08-30: Auto-price engine root-cause fix — P2P-zone (classify=profession) ads were advStatus=3 (offline) so applied ratios never reached the live book; engine now skips offline ads with skipped_reason 'ad_offline'. Also stopped self-chasing: our own advertiser nicknames (BlynkEx, ASEC-CORPORATION) are excluded from competitor matching. Verified live: 18:47 cycle logged ad_offline, 18:48 cycle applied with no BlynkEx self-matches.
- 2026-08-31: Holiday-aware attendance truth — hr_attendance_day_v now maps punch-less days that fall on an active hr_holidays date (exact or recurring month/day) to status 'holiday' with zero LOP, via new hr_is_holiday(date). Fixes 15-Aug and 28-Aug (Rakhi) rendering as blank/no_data across the HRMS calendar and ESS surfaces. Calendar half-day chip relabelled "½D" (was "H", misread as Holiday).
- 2026-08-31: Attendance Calendar blank-employee root cause fixed — the all-employee month RPC exceeded PostgREST's 1,000-row response cap, so later employees lost every day despite existing records; the sanctioned attendance reader now retrieves all deterministically ordered pages.
- 2026-08-31: Non-HRMS reporting-manager approvals repaired — ERP Profile → Requests now reads manager-scoped leave/regularization queues, exposes load failures instead of silently hiding them, and scroll-highlights the exact request opened from a notification.
- 2026-08-31: Manager visibility fix — added hr_manager_direct_reports() RPC + "Reports to you" section in ERP profile My Team card; regularization card now explains requests still parked with HR. Shivendra Namdev's 8 reports and 1 pending leave verified present; his 4 team regularizations were unpushed by HR (workflow, not bug).
- 2026-09-01: Attendance integrity fix — v4 engine now persists an OPEN session when a stale forgotten punch-in is dropped by the watchdog gap (previously the punch and day evidence vanished), engine-derived 'incomplete' can no longer linger once evidence is gone, stale legacy 'incomplete' mirrors are cleaned, and August was recomputed for affected employees. ERP profile calendar no longer paints 'incomplete'/'in_progress' as Present — it now matches HRMS.
- 2026-09-01: Leave accrual automation live — CL 1 day on the 10th monthly (from joining), SL 6 days on first 10th after probation then every 12 months; daily cron 04:30 UTC (10:00 IST); probation guard now date-aware; leave consumption fixed to read the yearly accrual bucket.
- 2026-09-02: Backfilled missed August casual-leave accrual — 1 CL credited (dated 10-Aug-2026) to 35 active employees who joined before 10 Aug 2026; logged in hr_leave_accrual_log so the daily engine will not repeat it.
- 2026-09-02: Shift data integrity — added two-way DB sync triggers between hr_employee_work_info.shift_id and hr_employee_shift_schedule so any shift reassignment (schedule manager, employee-list bulk action, profile work-info edit) reflects in the Employee List Shift column. Verified both directions via rollback self-test. Badge 11 (Syed Adnan Imran) has no shift assigned anywhere — shows 'None' correctly.
- 2026-09-02 (IST): Verified live against RazorpayX (Opfin) that the WORK EMAIL is the person's identity key and is read-only over the Payroll API — every people:edit spelling (new-email / work-email / email-id / new_email / people-id keyed) returns HTTP 200 while keeping the old value, and people:{edit,change,update}-email return code 23. Proxy now drops the email from the payload, pushes all remaining fields, and reports 'pushed_email_dashboard_only'; UI shows a 'Dashboard only' section instead of a failed push. Poonam Dahayat (RP ID 29): phone + employment type pushed; poonamd@gmail.com → rekhasanad@gmail.com must be changed in the RazorpayX dashboard.
- 2026-09-02 (IST): Monthly Payroll Cockpit UI/UX overhaul (presentation-only, audit-driven) — sticky command bar with stage rail + progress bar + "you are here" anchor, split system/acknowledgement chips, collapsible settled steps, inline blocker list, grouped routine/diagnostics toolbox, 40px mobile actions; shared cockpit tool shell (full-bleed widths, month+tool context header); all hardcoded emerald/amber/blue/red utilities in payroll pages replaced with success/warning/info/destructive tokens; RazorpayX Diagnostics width cap and glyph removed. No query, RPC, gate, permission or workflow changed.
- 2026-09-02 (IST): Payroll LOP engine P0 fix — hr_lop_days was calling JSONB functions on hr_weekly_off_patterns.weekly_offs (integer[]) and errored on every call, so LOP silently resolved to nothing; it is now a thin delegate of hr_lop_days_window (native array weekly-offs, joining/relieving-date clipping, held-harmless, comp-off-free raw LOP). hr_attendance_month_summary no longer runs its own second formula off the legacy hr_attendance table — it returns the canonical engine's present/leave/held/LOP figures and keeps legacy late/early/OT + legacy_present_days side by side for drift visibility; no_biometric_signal now derives from real punch/session evidence. Recurring 29-Feb holidays no longer crash non-leap-year months. Verified live: Aug-2026 summary runs for all 42 active employees (24 working days, 34 with LOP, 155.5 LOP days total) with mid-month joiners correctly clipped. Nothing staged/pushed — August LOP deductions still un-generated.
- 2026-09-02 12:45 IST: Payroll cockpit remediation slice 2 — auto-LOP rows are now written by stable identity (update-by-id / insert, no label in the write key) and pushed rows that disagree with current attendance are flagged (`stale_pushed`, `pushed_stale` in the summary) instead of silently frozen; cockpit step 4 identifies LOP rows by `source='auto_lop'` (plus "loss of pay" wording) and a verified zero-LOP month now completes (Aug-2026 expected_lop_days=155.5, so it correctly stays incomplete); step 5 uses the real recovery vocabulary (scheduled/pushed/paid, skipped ignored, pending blocks); step 7 requires payslip emails actually sent; step 8 requires a usable (computed) shadow run; step 9 scopes drift by "existed by month end" instead of last overnight rescan (Aug drift_open now 0, the 2 open alerts belong to September). Shadow page no longer double-counts additions/LOP over `monthly_gross` and excludes un-imported employees from both sides of the comparison; salary-base sources are declared annual instead of guessed at Rs 1,00,000. Verified live via hr_cockpit_month_state for Aug/Jul. Close-month `skipped` bypass deliberately left untouched pending the owner's policy answer.
- 2026-09-02 13:20 IST: Joiner/leaver LOP corrected to Model B (RazorpayX pays the full month, isProRated:false). New SQL helper hr_employment_gap_working_days counts working days outside the employment window using the same weekly-off/holiday rules; generate-lop-deductions now charges absence LOP + not-employed days, divides by FULL-month working days (was the employment-clipped count, which inflated a joiner's day rate ~2.2x), labels the split ('X days absence, Y days not employed'), and skips employees not employed at all in the month. salaryBase.ts now takes the RazorpayX-mirrored salary as authority, flags a disagreeing local assignment, and demotes onboarding CTC to an ERROR (it can no longer drive money) — this was the July root cause. Verified live: Satyam Shukla (badge 55, RP 55, DOJ 13-Jul) July = 2 absence + 10 not-employed of 27 WD on Rs 9,194 base = Rs 4,086; the pushed Rs 6,667 row (built on the stale Rs 15,000 onboarding CTC) stays locked and now surfaces as stale_pushed — Rs 2,581 over-deducted in July, to be corrected in RazorpayX by hand. Aug affected: badges 11,12,13,16,19,21,25. Edge functions redeployed: generate-lop-deductions, compute-shadow-payroll, generate-compoff-encashment, hr-projected-salary-register.
- 2026-09-02 20:05 IST: Payroll LOP policy corrections applied per owner decisions — half-day extra penalty disabled (half_day_count_for_lop 2→0, removed 14 penalty days in Aug), regularised days no longer double-credited (present + held), full attended day no longer double-credited with overlapping approved leave, comp-off pool now consumes comp-off only when comp-off actually funded the leave (restored 2 days to badge 15). Verified: Aug 2026 LOP 155.50 → 150.50 across 42 employees, 34 with LOP; late penalty already counted 1/day (no change); close-month steps remain fully skippable by HR (owner decision).
- 2026-09-03 03:30 IST: Leave Allocations switched from quarterly to true monthly view — added hr_leave_allocations.month (backfilled from created_at for legacy manual rows), month selector replaces quarter selector, per-employee "credited this month" comes from hr_leave_accrual_log (exact accrual dates) + month-tagged manual allocations; balances remain cumulative. Verified: 35 Aug-2026 accrual rows, 0 manual rows missing month, legacy distribution Jul=132/Aug=7.
- 2026-09-03 03:35 IST: Exit checklist — "Relieving cum experience letter issued" now generates the relieving letter headlessly (src/lib/issueLetter.ts), files it under the employee's Documents + hr_documents_issued, ticks the item, and exposes an optional "Email the relieving letter" action (hr-doc-email). Document Studio flow untouched.
- 2026-09-03 04:35 IST: Deposits / Error Recovery ↔ F&F wired as one system — hr_employee_deposits gained fnf_settlement_id + fnf_state (none|reserved|closed), refund_status extended with 'withheld', ledger types withheld/reserved/released added; new hr_apply_fnf_deposit_reservations(settlement) reserves lines on draft save, releases on cancel and refuses lines owned by another live settlement; hr_close_fnf_sources now applies per-line refund/withheld decisions from breakdown.deposit_decisions (legacy full-refund fallback retained) and writes the withheld ledger row with the real withheld amount (was 0). fnfEngine converts day-based penalties via RazorpayX monthly salary ÷ month working days; F&F page shows every collected deposit as an editable refund/withhold line with mandatory reason; Deposit Management shows Reserved/Settled badges, locks F&F-owned rows and requires a reason on amount edits (appended to ledger). Verified by rolled-back live self-tests on Lokesh (deposits 7005f07b 9,000 security / cc892e4c 6,000 error recovery): reserve→partial refund 2,000/withheld 4,000, reserve→full withhold 6,000 (ledger: ff_refund -9,000, withheld 6,000), and cancel→both released to fnf_state=none with no settlement left behind; F&F state machine rejected the illegal cancelled→paid/draft transitions. No live rows changed.
- 2026-09-03 04:55 IST: F&F settlements are now deletable with a full unwind — new RPC hr_delete_fnf_settlement(settlement, reason) (reason mandatory, authenticated+service_role only) reopens every deposit/error recovery the settlement reserved or closed (balance restored, refund/withheld cleared, 'released' ledger entry carrying the reason), reopens loans it closed with outstanding recomputed from actually-paid repayments, un-applies its penalties, and unticks the "Full & Final settlement initiated" exit-checklist item; it refuses outright when razorpay_push_status='pushed' (must be removed in RazorpayX first) and never silently reactivates the employee. FnFSettlementPage gained a delete action + confirm dialog that states each consequence and reports what was reversed. Verified by rolled-back live self-tests on Lokesh: paid settlement deleted → 2 deposits reopened (9,000 + 6,000, ledger released rows), 1 penalty reopened, checklist unticked, settlement row gone; pushed guard raised the correct refusal. No live rows changed.
- 2026-09-03 13:25 IST: Data Health drift dismissals are now sticky — hr_drift_alerts gained acknowledged_at/by/note + ack_hrms_value/ack_razorpay_value/ack_essl_value; hr_drift_open hides acknowledged rows (new hr_drift_acknowledged view lists them), and hr-drift-scan preserves the acknowledgement on re-scan only while all three stored values still match, clearing it the moment HRMS or RazorpayX changes. Verified live: acknowledged alert leaves hr_drift_open (still_open=0, in_ack=1), ack persists on unchanged values and invalidates on a changed value; test ack reverted. RazorpayX email push stays hidden — re-verified live against the official API: a control job-title edit on employee 29 applied and reverted, while every email variant (email/new-email/work-email/personal-email/official-email, keyed by e-mail and by employee-id) returned 200 with the address unchanged even for a fresh non-duplicate address ⇒ Out of RazorpayX API Scope / Limitation, not the "email already exists" collision.
- 2026-09-03 13:32 IST: Second-employee control test of RazorpayX email edit — Shubham Singh (rzp id 70, shubhamsuresh2004@gmail.com) with fresh address sddiyfjn@gmail.com. Any payload containing "email" is treated as the LOOKUP key ⇒ code 8 "Unable to locate the user" (with employee-id, with e-mail, with both). Aliases e-mail/work-email/personal-email keyed by employee-id return {"status":"ok"} but people:view still shows the old address. Control title edit on the same record returned ok. Nothing was altered; no revert needed. Confirms email is Out of RazorpayX API Scope / Limitation for all employees, not a Poonam-specific duplicate-email issue.
- 2026-09-03 13:35 IST: F&F deposits/error recovery — written reason is now mandatory in the database (trigger on hr_fnf_settlements + hr_close_fnf_sources guard) whenever money is not paid back in full; refund_status now records 'partial'; employee profile shows a permanent exit record (paid back / kept / reason / date / F&F ref) that survives deactivation.
- 2026-09-03 13:52 IST: Stale-session resolver made idempotent (ON CONFLICT on duplicate out-punch; regularized days close as 'resolved_regularized'; orphaned entries auto-closed). Cleared the 3 stuck rows — queue now 0 open.
- 2026-09-03 13:58 IST: ERP terminal SALES approval unblocked — the userNo resolver effect depended on the periodically-refetched clients list, so each refetch cancelled the in-flight resolution and re-armed 'Inferring Binance User No…', permanently disabling Approve. Resolution now runs once per order (clients read via ref).
- 2026-09-03 14:05 IST: Fixed off-market/ERP sales & purchase entry client search — cached full-list fetch could truncate (paginator now advances by rows actually returned, stable name+id sort) and both autocompletes now also run a live server-side ilike search (name/phone/PAN, limit 50) merged into matching, so existing older clients (e.g. Sanyam Jain) are found instead of being flagged as new.
- 2026-09-03 14:28 IST: F&F settlements now carry an explicit payroll cycle month (hr_fnf_settlements.payroll_month, backfilled from LWD month). hr-push-fnf pushes into that cycle and mirrors the two consolidated lines into hr_payroll_input_additions/deductions with source='fnf_settlement' (marked pushed), shown in the cockpit Additions/Deductions step under a segregated "F&F settlement" card.
- 2026-09-03 14:40 IST: Separation is no longer completed from the exit checklist (the "Complete Resignation & Deactivate" button is removed — dismissing in RazorpayX before the final run blocks payroll). Marking the F&F settlement paid now requires razorpay_push_status pushed/nothing_to_push and finalises the separation itself via src/lib/finalizeSeparation.ts (resignation_status=completed, is_active=false, ERP login disabled, eSSL removal, RazorpayX dismissal prompt).
- 2026-09-03 14:55 IST: F&F create/edit dialog extracted to shared FnFSettlementDialog; exit-checklist "Create F&F settlement" now opens the same dialog in place (fixed employee, auto-filled), saved settlements flow through the identical F&F page pipeline.
- 2026-09-03 15:05 IST: Monthly Payroll Cockpit now has 11 steps — new Step 3 "Separations & Full & Final for this cycle" (existing steps 3-10 renumbered 4-11, prior acknowledgements shifted with them; close-month checks steps 1-10 and acks step 11). Step 3 opens SeparationsFnFPanel: F&F settlements tagged to the cycle (edit via the shared dialog), in-month exits without a settlement (create inline), mis-tagged cycle retag, and inline resignation initiation. Verified: July acks moved 4-11, Sep-2026 counts fnf_total=1/fnf_open=1/exits=1/exits_without_fnf=0.
- 2026-09-03 15:20 IST: Exit reason now flows into the RazorpayX dismissal path — finalizeSeparation returns separation_reason, the F&F "dismiss in Razorpay" prompt shows it and passes it instead of the hardcoded "F&F settled", and dismissInRazorpay stores it on the pushback audit row + manual-dismissal drift note. RazorpayX people:dismiss itself accepts only {email,dateOfDismissal} (reason field = Out of RazorpayX API Scope / Limitation, verified 2026-08-13), so the reason is HRMS-side only and must be typed in the dashboard if needed there.
- 2026-09-03 15:25 IST: HRMS mobile optimization — added phone-layer CSS under .horilla-root (header stacking, 2-col metric grids, tablist scroll, 16px inputs, dialog fit, tighter padding) + F&F settlement card responsive header/grid. Presentation-only.
- 2026-09-03 16:25 IST: Fixed HRMS mobile dashboard heading overlap — global phone CSS wrap rules were wrapping HorillaHeader (menu/search/icons) onto 2 rows inside fixed h-12 header, spilling the search bar over the page title; added header-only nowrap exception + 16px input (iOS zoom guard) in src/index.css. Presentation only.
